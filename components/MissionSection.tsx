import MissionSectionClient from "./MissionSection.client";

export default function MissionSection() {
  // Server wrapper: render the interactive client component.
  return <MissionSectionClient />;
}
